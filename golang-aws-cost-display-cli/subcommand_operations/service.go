package subcommand_operations

func CheckServiceCmdFlags(name, month, between string) error {
	// validate the name flag is provided and is a valid AWS service

	// validate that either month or between is provided, but not both

	// if only name if provided, default to current month

	// validate the month flag is in correct format if provided in yyyy-mm

	// validate if the between flag is in correct format if provided in yyyy-mm-dd, yyyy-mm-dd

	// return nil if all validations pass, otherwise return an error with appropriate message

	//
	return nil
}
